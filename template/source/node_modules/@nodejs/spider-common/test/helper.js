/**
 * @author yangyufei
 * @date 2019-02-26 17:50:24
 * @desc
 */
const {expect}      = require('chai');

module.exports = async cb => {
    let pass = true;
    try {
        pass = await cb();
    } catch(err) {
        console.error(err);
        pass = false;
    }

    expect(pass).to.be.equal(true);
};