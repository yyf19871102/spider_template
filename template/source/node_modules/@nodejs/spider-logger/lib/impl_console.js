/**
 * @author yangyufei
 * @date 2019-02-28 17:29:05
 * @desc
 */
const _         = require('lodash');
const chalk     = require('chalk');

const settings  = require('./settings');
const {level}   = settings;

module.exports = () => {
    const logger = {};

    _.keys(level).forEach(levelName => {
        logger[levelName] = msgOrObj => {
            let msg = settings.makeMessage(level[levelName].name, msgOrObj);

            console.log(chalk.hex(level[levelName].color)(msg));
        }
    });

    return logger;
};