/**
 * @author yangyufei
 * @date 2019-02-28 18:58:07
 * @desc
 */
const assert    = require('assert')

module.exports = async cb => {
    let pass = true;
    try {
        pass = await cb();
    } catch(err) {
        console.error(err);
        pass = false;
    }

    assert.equal(pass, true);
};