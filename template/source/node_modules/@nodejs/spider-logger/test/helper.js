/**
 * @author yangyufei
 * @date 2019-02-28 18:58:07
 * @desc
 */
const {expect}      = require('chai');

module.exports = async cb => {
    let pass = true;
    try {
        pass = await cb();
    } catch(err) {
        console.error(err);
        pass = false;
    }

    expect(pass).to.be.equal(true);
};